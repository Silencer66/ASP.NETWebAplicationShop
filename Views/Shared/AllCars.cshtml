@model Car

<div class="col-lg-4">
    <img class="img-thumbnail" src="@Model.img" alt="@Model.carName" />
    <h2>@Model.carName</h2>
    <p>@Model.shortDescription</p>
    <p>Цена: @Model.carPrice.ToString("c")</p>
    <p><a class="btn btn-warning" href="#">Подробнее</a></p>
</div>